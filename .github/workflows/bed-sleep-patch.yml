name: Bed sleep runtime integration patch
on:
  push:
    branches: [survival/v0.4-bed-sleep-night-skip]
permissions:
  contents: write
jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          ref: survival/v0.4-bed-sleep-night-skip
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.13'
      - run: python scripts/patch-bed-sleep-runtime.py
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: |
          node --check src/main.js
          node --check tests/e2e/smoke.spec.mjs
          node scripts/check-sleep.mjs
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/main.js tests/e2e/smoke.spec.mjs
          git commit -m "survival: sleep in beds to skip the night"
          git push origin HEAD:refs/heads/survival/v0.4-bed-sleep-night-skip
