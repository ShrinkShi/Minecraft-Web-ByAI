import {assertClientSessionId} from './client-input-envelope.js';
import {assertNetworkSequence} from './network-sequence.js';
import {FURNACE_SLOT} from './smelting.js';
import {normalizeFurnaceTarget} from './server-furnace-container-snapshot.js';

export const FURNACE_TRANSACTION_PROTOCOL_VERSION=1;
export const FURNACE_TRANSACTION_REQUEST_KIND='furnace-transaction-request';
export const FURNACE_TRANSACTION_RESULT_KIND='furnace-transaction-result';
export const FURNACE_TRANSACTION_ACTIONS=Object.freeze(['slot-click','take-output','close']);
const ACTION_SET=new Set(FURNACE_TRANSACTION_ACTIONS);
function object(value,label){if(!value||typeof value!=='object'||Array.isArray(value))throw new TypeError(`${label} must be an object`);return value;}
function exactKeys(value,keys,label){const actual=Object.keys(value).sort(),expected=[...keys].sort();if(actual.length!==expected.length||actual.some((key,index)=>key!==expected[index]))throw new RangeError(`${label} fields are invalid`);}
function slot(value){if(value!==FURNACE_SLOT.INPUT&&value!==FURNACE_SLOT.FUEL)throw new RangeError('furnace transaction slot must be input or fuel');return value;}
function button(value){if(value!==0&&value!==2)throw new RangeError('furnace transaction button must be 0 or 2');return value;}
function bool(value,label){if(typeof value!=='boolean')throw new TypeError(`${label} must be a boolean`);return value;}
function code(value){if(typeof value!=='string'||!/^[a-z0-9-]{1,64}$/u.test(value))throw new RangeError('furnace transaction result code must be 1 to 64 lowercase ASCII characters, digits, or hyphens');return value;}
function version(value){if(value!==FURNACE_TRANSACTION_PROTOCOL_VERSION)throw new RangeError(`furnace transaction version must be ${FURNACE_TRANSACTION_PROTOCOL_VERSION}`);return value;}

export function normalizeFurnaceTransactionAction(value){value=object(value,'furnace transaction action');const type=value.type;if(!ACTION_SET.has(type))throw new RangeError(`unsupported furnace transaction action: ${type}`);if(type==='slot-click'){exactKeys(value,['type','slot','button','shift'],'furnace slot-click action');return Object.freeze({type,slot:slot(value.slot),button:button(value.button),shift:bool(value.shift,'furnace slot-click shift')});}if(type==='take-output'){exactKeys(value,['type','button','shift'],'furnace take-output action');return Object.freeze({type,button:button(value.button),shift:bool(value.shift,'furnace take-output shift')});}exactKeys(value,['type'],'furnace close action');return Object.freeze({type});}
export function encodeFurnaceTransactionRequest({session,requestId,target,expectedInventoryRevision,expectedContainerRevision,action}={}){return Object.freeze({v:FURNACE_TRANSACTION_PROTOCOL_VERSION,kind:FURNACE_TRANSACTION_REQUEST_KIND,session:assertClientSessionId(session),requestId:assertNetworkSequence(requestId,'furnace transaction requestId'),target:normalizeFurnaceTarget(target),expectedInventoryRevision:assertNetworkSequence(expectedInventoryRevision,'furnace expectedInventoryRevision'),expectedContainerRevision:assertNetworkSequence(expectedContainerRevision,'furnace expectedContainerRevision'),action:normalizeFurnaceTransactionAction(action)});}
export function decodeFurnaceTransactionRequest(raw,{expectedSession=null}={}){raw=object(raw,'furnace transaction request');exactKeys(raw,['v','kind','session','requestId','target','expectedInventoryRevision','expectedContainerRevision','action'],'furnace transaction request');version(raw.v);if(raw.kind!==FURNACE_TRANSACTION_REQUEST_KIND)throw new RangeError(`furnace transaction request kind must be ${FURNACE_TRANSACTION_REQUEST_KIND}`);const session=assertClientSessionId(raw.session);if(expectedSession!==null&&session!==assertClientSessionId(expectedSession))throw new RangeError('furnace transaction request session mismatch');return encodeFurnaceTransactionRequest({session,requestId:raw.requestId,target:raw.target,expectedInventoryRevision:raw.expectedInventoryRevision,expectedContainerRevision:raw.expectedContainerRevision,action:raw.action});}
export function encodeFurnaceTransactionResult({session,requestId,target,ok,code:resultCode,inventoryRevision,containerRevision}={}){if(typeof ok!=='boolean')throw new TypeError('furnace transaction result ok must be a boolean');return Object.freeze({v:FURNACE_TRANSACTION_PROTOCOL_VERSION,kind:FURNACE_TRANSACTION_RESULT_KIND,session:assertClientSessionId(session),requestId:assertNetworkSequence(requestId,'furnace transaction result requestId'),target:normalizeFurnaceTarget(target),ok,code:code(resultCode),inventoryRevision:assertNetworkSequence(inventoryRevision,'furnace transaction result inventoryRevision'),containerRevision:assertNetworkSequence(containerRevision,'furnace transaction result containerRevision')});}
export function decodeFurnaceTransactionResult(raw,{expectedSession=null}={}){raw=object(raw,'furnace transaction result');exactKeys(raw,['v','kind','session','requestId','target','ok','code','inventoryRevision','containerRevision'],'furnace transaction result');version(raw.v);if(raw.kind!==FURNACE_TRANSACTION_RESULT_KIND)throw new RangeError(`furnace transaction result kind must be ${FURNACE_TRANSACTION_RESULT_KIND}`);const session=assertClientSessionId(raw.session);if(expectedSession!==null&&session!==assertClientSessionId(expectedSession))throw new RangeError('furnace transaction result session mismatch');return encodeFurnaceTransactionResult({session,requestId:raw.requestId,target:raw.target,ok:raw.ok,code:raw.code,inventoryRevision:raw.inventoryRevision,containerRevision:raw.containerRevision});}
