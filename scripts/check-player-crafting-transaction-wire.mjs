import assert from 'node:assert/strict';
import {decodePlayerCraftingTransactionRequest,decodePlayerCraftingTransactionResult,encodePlayerCraftingTransactionRequest,encodePlayerCraftingTransactionResult,PLAYER_CRAFTING_TRANSACTION_REQUEST_KIND,PLAYER_CRAFTING_TRANSACTION_RESULT_KIND} from '../src/player-crafting-transaction-wire.js';

const session='s:crafting-wire';
const request=encodePlayerCraftingTransactionRequest({session,requestId:5,expectedInventoryRevision:11,expectedCraftingRevision:4,action:{type:'input-click',slot:2,button:2,shift:false}});
assert.deepEqual(request,{v:1,kind:PLAYER_CRAFTING_TRANSACTION_REQUEST_KIND,session,requestId:5,expectedInventoryRevision:11,expectedCraftingRevision:4,action:{type:'input-click',slot:2,button:2,shift:false}});assert.deepEqual(decodePlayerCraftingTransactionRequest(structuredClone(request),{expectedSession:session}),request);
assert.deepEqual(encodePlayerCraftingTransactionRequest({session,requestId:6,expectedInventoryRevision:11,expectedCraftingRevision:4,action:{type:'take-result',shift:true}}).action,{type:'take-result',shift:true});assert.deepEqual(encodePlayerCraftingTransactionRequest({session,requestId:7,expectedInventoryRevision:11,expectedCraftingRevision:4,action:{type:'close'}}).action,{type:'close'});
assert.throws(()=>decodePlayerCraftingTransactionRequest({...request,result:{id:'block:5',count:4}}),/fields are invalid/,'client must never submit a trusted crafted result');assert.throws(()=>encodePlayerCraftingTransactionRequest({session,requestId:0,expectedInventoryRevision:0,expectedCraftingRevision:0,action:{type:'input-click',slot:4,button:0,shift:false}}),/0 to 3/);assert.throws(()=>encodePlayerCraftingTransactionRequest({session,requestId:0,expectedInventoryRevision:0,expectedCraftingRevision:0,action:{type:'take-result',shift:1}}),/boolean/);
const result=encodePlayerCraftingTransactionResult({session,requestId:5,ok:true,code:'crafted',inventoryRevision:12,craftingRevision:5});assert.deepEqual(result,{v:1,kind:PLAYER_CRAFTING_TRANSACTION_RESULT_KIND,session,requestId:5,ok:true,code:'crafted',inventoryRevision:12,craftingRevision:5});assert.deepEqual(decodePlayerCraftingTransactionResult({...result},{expectedSession:session}),result);assert.throws(()=>decodePlayerCraftingTransactionResult({...result,craftingRevision:-1}),/uint32/);
console.log('strict dual-revision player crafting transaction wire contract: PASS');
