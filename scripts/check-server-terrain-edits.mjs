import assert from 'node:assert/strict';
import {BLOCK,CHUNK_SIZE} from '../src/blocks.js';
import {terrainChunkIndex} from '../src/terrain-generator.js';
import {ServerTerrainWorld} from '../server/terrain-world.mjs';

const mod=(value,divisor)=>((value%divisor)+divisor)%divisor;
const world=new ServerTerrainWorld({seed:'golden-seed',prompt:'mountain forest',maxCacheChunks:4});
assert.equal(world.revision,0);assert.equal(world.editCount,0);assert.equal(world.stateEditCount,0);assert.throws(()=>{world.revision=99;},TypeError);
const baseGround=world.highestSolid(33,-17);assert.equal(baseGround,22);const editY=baseGround+1,baseDecoration=world.getBaseBlock(33,editY,-17);assert.equal(baseDecoration,BLOCK.SHORT_GRASS,'current terrain v4 server baseline must expose deterministic short grass at the locked vegetation coordinate');assert.equal(world.getBlock(33,editY,-17),baseDecoration);

let change=world.setBlock(33,editY,-17,BLOCK.STONE);assert.deepEqual(change,{changed:true,revision:1,x:33,y:23,z:-17,previous:baseDecoration,previousStateKey:null,id:BLOCK.STONE,stateKey:null,base:baseDecoration,storedEdit:true,storedStateEdit:false});assert.equal(Object.isFrozen(change),true);assert.equal(world.revision,1);assert.equal(world.editCount,1);assert.equal(world.getBaseBlock(33,editY,-17),baseDecoration,'seed baseline must remain immutable');assert.equal(world.getBlock(33,editY,-17),BLOCK.STONE);assert.deepEqual(world.getBlockState(33,editY,-17),{id:BLOCK.STONE,stateKey:null});assert.equal(world.isSolidBlock(33,editY,-17),true);assert.equal(world.isLiquidBlock(33,editY,-17),false);assert.equal(world.highestSolid(33,-17),23);
change=world.setBlock(33,editY,-17,BLOCK.STONE);assert.equal(change.changed,false);assert.equal(change.revision,1,'idempotent set must not advance revision');assert.equal(change.previousStateKey,null);assert.equal(change.stateKey,null);assert.equal(world.editCount,1);

const cx=Math.floor(33/CHUNK_SIZE),cz=Math.floor(-17/CHUNK_SIZE),lx=mod(33,CHUNK_SIZE),lz=mod(-17,CHUNK_SIZE);let chunk=world.getChunkSnapshot(cx,cz);assert.equal(chunk[terrainChunkIndex(lx,editY,lz)],BLOCK.STONE,'chunk snapshots must include authoritative edits');chunk[terrainChunkIndex(lx,editY,lz)]=BLOCK.WATER;assert.equal(world.getBlock(33,editY,-17),BLOCK.STONE,'chunk snapshot remains isolated from authoritative edits');world.clearCache();assert.equal(world.getBlock(33,editY,-17),BLOCK.STONE,'LRU cache clear must not lose edits');

change=world.setBlock(33,editY,-17,BLOCK.WATER);assert.equal(change.revision,2);assert.equal(change.previous,BLOCK.STONE);assert.equal(change.previousStateKey,null);assert.equal(change.stateKey,null);assert.equal(change.storedEdit,true);assert.equal(world.isSolidBlock(33,editY,-17),false);assert.equal(world.isLiquidBlock(33,editY,-17),true);assert.equal(world.highestSolid(33,-17),22,'non-solid edited water must not become ground');
change=world.setBlock(33,editY,-17,baseDecoration);assert.equal(change.revision,3);assert.equal(change.storedEdit,false,'returning to the generated v4 baseline must compact the sparse edit');assert.equal(change.storedStateEdit,false);assert.equal(world.editCount,0);assert.equal(world.getBlock(33,editY,-17),baseDecoration);assert.equal(world.highestSolid(33,-17),22,'restored short grass remains non-solid');assert.deepEqual(world.exportEdits(),{});assert.deepEqual(world.exportBlockStates(),{});

const nx=-1,nz=-1,ny=10,negativeBase=world.getBaseBlock(nx,ny,nz),negativeEdit=negativeBase===BLOCK.AIR?BLOCK.STONE:BLOCK.AIR;change=world.setBlock(nx,ny,nz,negativeEdit);assert.equal(change.revision,4);assert.equal(world.getBlock(nx,ny,nz),negativeEdit);assert.equal(world.editCount,1);assert.deepEqual(world.editEntries(),[{x:-1,y:10,z:-1,id:negativeEdit,stateKey:null}]);assert.equal(Object.isFrozen(world.editEntries()[0]),true);assert.deepEqual(world.exportEdits(),{'-1,10,-1':negativeEdit});const negativeChunk=world.getChunkSnapshot(-1,-1);assert.equal(negativeChunk[terrainChunkIndex(15,ny,15)],negativeEdit,'negative world coordinates must overlay the correct local chunk cell');

const secondBase=world.getBaseBlock(1,24,2),secondEdit=secondBase===BLOCK.STONE?BLOCK.AIR:BLOCK.STONE;world.setBlock(1,24,2,secondEdit);assert.equal(world.revision,5);assert.deepEqual(world.editEntries(),[{x:-1,y:10,z:-1,id:negativeEdit,stateKey:null},{x:1,y:24,z:2,id:secondEdit,stateKey:null}],'edit export order must be deterministic');const exported=world.exportEdits();exported['1,24,2']=BLOCK.WATER;assert.equal(world.getBlock(1,24,2),secondEdit,'exported edits must not expose mutable server state');

assert.throws(()=>world.setBlock(0,-1,0,BLOCK.AIR),/wy must be from/);assert.throws(()=>world.setBlock(0,64,0,BLOCK.STONE),/wy must be from/);assert.throws(()=>world.setBlock(0,10,0,999),/known block/);assert.throws(()=>world.setBlock(.5,10,0,BLOCK.AIR),/wx must be an integer/);assert.throws(()=>world.setBlockStateKey(0,10,0,BLOCK.LOG,'axis=q'),/axis/);

world.setBlock(nx,ny,nz,negativeBase);world.setBlock(1,24,2,secondBase);assert.equal(world.editCount,0);assert.equal(world.revision,7);assert.deepEqual(world.exportEdits(),{});

const sx=7,sy=63,sz=7,stateBase=world.getBaseBlock(sx,sy,sz),stateId=stateBase===BLOCK.LOG?BLOCK.FURNACE:BLOCK.LOG,defaultStateKey=stateId===BLOCK.LOG?'axis=y':'facing=north',alternateStateKey=stateId===BLOCK.LOG?'axis=x':'facing=east';
change=world.setBlockStateKey(sx,sy,sz,stateId,defaultStateKey);assert.equal(change.changed,true);assert.equal(change.revision,8);assert.equal(change.stateKey,defaultStateKey);assert.equal(change.storedEdit,true);assert.equal(change.storedStateEdit,false,'canonical default state does not require a sidecar override');assert.equal(world.editCount,1);assert.equal(world.stateEditCount,0);
change=world.setBlockStateKey(sx,sy,sz,stateId,alternateStateKey);assert.equal(change.changed,true,'same block id with a different state must still be an authoritative edit');assert.equal(change.revision,9);assert.equal(change.previous,stateId);assert.equal(change.previousStateKey,defaultStateKey);assert.equal(change.id,stateId);assert.equal(change.stateKey,alternateStateKey);assert.equal(change.storedEdit,true);assert.equal(change.storedStateEdit,true);assert.equal(world.editCount,1,'state mutation must not duplicate coordinate accounting');assert.equal(world.stateEditCount,1);assert.deepEqual(world.getBlockState(sx,sy,sz),{id:stateId,stateKey:alternateStateKey});assert.deepEqual(world.editEntries(),[{x:sx,y:sy,z:sz,id:stateId,stateKey:alternateStateKey}]);assert.deepEqual(world.exportBlockStates(),{[`${sx},${sy},${sz}`]:{id:stateId,stateKey:alternateStateKey}});
change=world.setBlock(sx,sy,sz,stateId);assert.equal(change.changed,true,'legacy setBlock must reset stateful blocks to their canonical default');assert.equal(change.revision,10);assert.equal(change.previousStateKey,alternateStateKey);assert.equal(change.stateKey,defaultStateKey);assert.equal(change.storedStateEdit,false);assert.equal(world.stateEditCount,0);
change=world.setBlock(sx,sy,sz,stateBase);assert.equal(change.changed,true);assert.equal(change.revision,11);assert.equal(change.storedEdit,false);assert.equal(change.storedStateEdit,false);assert.equal(world.editCount,0);assert.equal(world.stateEditCount,0);assert.deepEqual(world.exportEdits(),{});assert.deepEqual(world.exportBlockStates(),{});

console.log('mutable sparse server terrain v4 edits + block-state identities + revision/chunk/environment contracts: PASS');
