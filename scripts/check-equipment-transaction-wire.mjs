import assert from 'node:assert/strict';
import {decodeEquipmentTransactionRequest,decodeEquipmentTransactionResult,encodeEquipmentTransactionRequest,encodeEquipmentTransactionResult,EQUIPMENT_TRANSACTION_REQUEST_KIND,EQUIPMENT_TRANSACTION_RESULT_KIND} from '../src/equipment-transaction-wire.js';

const session='s:equipment-transaction-wire';
const request=encodeEquipmentTransactionRequest({session,requestId:7,expectedInventoryRevision:11,expectedEquipmentRevision:3,slot:'head',button:0});
assert.deepEqual(request,{v:1,kind:EQUIPMENT_TRANSACTION_REQUEST_KIND,session,requestId:7,expectedInventoryRevision:11,expectedEquipmentRevision:3,slot:'head',button:0});assert.deepEqual(decodeEquipmentTransactionRequest(structuredClone(request),{expectedSession:session}),request);
assert.throws(()=>decodeEquipmentTransactionRequest({...request,session:'s:other'},{expectedSession:session}),/session mismatch/);assert.throws(()=>decodeEquipmentTransactionRequest({...request,result:{id:'leather_helmet',count:1}}),/fields are invalid/,'client must not submit trusted equipment result state');assert.throws(()=>encodeEquipmentTransactionRequest({session,requestId:0,expectedInventoryRevision:0,expectedEquipmentRevision:0,slot:'body',button:0}),/unsupported equipment slot/);assert.throws(()=>encodeEquipmentTransactionRequest({session,requestId:0,expectedInventoryRevision:0,expectedEquipmentRevision:0,slot:'head',button:1}),/0 or 2/);
const result=encodeEquipmentTransactionResult({session,requestId:7,ok:true,code:'equipped',inventoryRevision:12,equipmentRevision:4});assert.deepEqual(result,{v:1,kind:EQUIPMENT_TRANSACTION_RESULT_KIND,session,requestId:7,ok:true,code:'equipped',inventoryRevision:12,equipmentRevision:4});assert.deepEqual(decodeEquipmentTransactionResult({...result},{expectedSession:session}),result);assert.throws(()=>decodeEquipmentTransactionResult({...result,inventoryRevision:-1}),/uint32/);assert.throws(()=>encodeEquipmentTransactionResult({session,requestId:7,ok:false,code:'BAD CODE',inventoryRevision:12,equipmentRevision:4}),/lowercase ASCII/);
console.log('strict dual-revision equipment transaction wire contract: PASS');
